# moving-target-defense-chat
A Python-based secure chat application implementing Moving Target Defense (MTD). The server rotates ports every 10 seconds using HMAC-SHA256 to evade network scanning and unauthorized connections.
